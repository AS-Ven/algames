export type TcompleteUser = {
    id: number,
    name: string,
    password: string,
    title: string,
    icon: number,
    matching_marks: number[]
}

export type Tuser = {
    name: string,
    title: string,
    matching_marks: number[]
}